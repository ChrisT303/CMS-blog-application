<div class="card">
  <div class="card-header  flex-row justify-space-between">
    <h2>
      <a href="/post/{{post.id}}">
        <div class="col-8">{{post.post_title}}</div>
      </a>
    </h2>
    <div class="col-3">posted by {{post.user.username}} on {{format_date post.date}}
    </div>
  </div>  
  <p class="card-body">
   {{post.post_content}}
  </p>
</div>
<input type="text" id="post-id" value="{{post.id}}"/>
<div class="card">
  <div class="card-header"></div>
  <form class="card-body" id="new-comment">
    <p class="form-label">Comment:</p>
    <textarea class="form-input" id="comment-text"></textarea>
    <button type="submit" class="btn btn-primary">Submit:</button>
  </form>
{{> comment post.comment}}
</div>
<script src="/Javascript/comment.js"></script>